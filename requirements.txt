requests>=2.28
# optional tiers (install via extras instead, see pyproject.toml):
#   local tier  -> mlx-lm        ("triple-stack-llm[local]", Apple Silicon)
#   claude tier -> anthropic     ("triple-stack-llm[claude]")
